declare namespace zim {
	interface BlobCfg {
		points ?: number ,
		color ?: string,
	}

	class Container extends createjs.Container{
		addTo(container : createjs.Container)
	}
	//extends a zim.Container which extends a createjs.Container
	export class Blob extends Container{
		constructor(blobCfg: BlobCfg);

	}
}