import Bitmap = createjs.Bitmap;

class wand extends Bitmap implements IDispose{
	constructor(imageOrUrl: HTMLImageElement | HTMLCanvasElement | HTMLVideoElement | Object | string){
		super(imageOrUrl);
	}

	breath(){

	}
	unBreath(){

	}

	dispose(){

	}
}